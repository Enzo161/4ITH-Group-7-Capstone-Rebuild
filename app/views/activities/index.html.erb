<p id="notice"><%= notice %></p>

<h1>Activities</h1>

<table class="table table-hover table-bordered ">
  <thead>
    <tr>
      <th>Time</th>
      <th>Key</th>
      <th>User</th>
      <th>Object</th>
    </tr>
  </thead>

  <tbody>
    <% @activities.each do |a| %>
      <tr>
        <td><%= a.created_at %></td>
        <td><%= a.key %></td>
        <td><%= a.owner&.email %></td>
        <td>
          <% if a.trackable&.name = nil || a.key != 'island.destroy' && a.trackable_type == 'Island' %>
            <%= a.trackable&.name %>
          <% else %>
            <%= "This has been deleted" %>
          <% end %>
          <%= a.trackable if a.trackable_type == 'Client' %>
          <%= a.trackable if a.trackable_type == 'User' %>
        </td>
        <!-- <td><%#= a.key != "island.destroy" %></td>
        <td><%#= link_to 'Show', activity %></td>
        <td><%#= link_to 'Edit', edit_activity_path(activity) %></td>
        <td><%#= link_to 'Destroy', activity, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>